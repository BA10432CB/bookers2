<div>
  <%= flash[:notice] %>
</div>

<% if @book.errors.any? %>
  <% if @book.errors.count > 1 %>
    <%= @book.errors.count %>errors prohibited this user from being saved:
  <% else %>
    <%= @book.errors.count %>error prohibited this user from being saved:
  <% end %>
  <ul>
    <% @book.errors.full_messages.each do |message| %>
     <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'list', user: @user, book: @book %>

    <div class='col-md-8 offset-md-1'>
      <h2>Books</h2>
      <table class='table table-hover table-inverse'>
        <thead class='book-nav'>
          <tr>
            <th></th>
            <th>Title<th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>
<!--登録者全員の全投稿-->
        <tbody class="book-index">
         <%= render 'book_list', books: @books %>
        </tbody>
      </table>
    </div>
  </div>
</div>