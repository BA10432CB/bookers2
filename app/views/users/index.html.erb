<%= render 'books/list', user: @user, book: @book %>
    <div class='col-md-8 offset-md-1'>
      <h1>Users</h1>
      <table class="table">
        <thead>
          <th>image</th>
          <th>name</th>
          <th colspan="3"></th>
        </thead>

<!--登録されたユーザー一覧表示-->
        <tbody>
          <%= render 'index', users: @users %>
        </tbody>
      </table>
    </div>
  </div>
</div>
